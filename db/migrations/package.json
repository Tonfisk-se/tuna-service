{
  "name": "migrations",
  "version": "1.0.0",
  "description": "service for handling tuna db migrations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "set-database-url": "set -a . ../../.env.development set +a",
    "migrate:up": "npm run set-database-url && node-pg-migrate up",
    "migrate:down": "npm run set-database-url && node-pg-migrate down",
    "migrate:redo": "npm run set-database-url && node-pg-migrate redo",
    "migrate:create": "npm run set-database-url && node-pg-migrate create"
  },
  "author": "johanfant",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "node-pg-migrate": "^7.9.0"
  }
}
